[defaults]
interpreter_python = /usr/bin/python3
# Default remote user. Overridden by the --user flag in run-playbook.bash.
remote_user = ubuntu
host_key_checking = False

[inventory]
enable_plugins = host_list
